import { ScrollReverserDirective } from './scroll-reverser.directive';

describe('ScrollReverserDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollReverserDirective();
    expect(directive).toBeTruthy();
  });
});
